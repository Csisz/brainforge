node_modules/
.next/
.env*.local
demo-output/*.png

# Build artifact — regenerated by `npm run typecheck`, never reviewed.
tsconfig.tsbuildinfo

# Personal machine config (Claude Code permissions), not project code.
.claude/settings.local.json

# Scratch renders from the sprint patch drops; demo-output/ is the tracked
# sample set (`npm run demo:worksheets`).
demo-samples/
