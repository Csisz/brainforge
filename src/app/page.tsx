/**
 * Placeholder — the landing page is Sprint 2 (design plan in README §Sprint 2).
 * Kept minimal deliberately: no throwaway UI to delete later.
 */
export default function Home() {
  return (
    <main className="grid min-h-screen place-items-center p-8">
      <p className="text-ink-soft">BrainForge Kids AI — engine online. UI arrives in Sprint 2.</p>
    </main>
  );
}
